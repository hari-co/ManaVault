import { CardType } from "@/types/CardType";

const CardProperties: React.FC<{card: CardType}> = ({ card }) => {
    return (
        <div className="flex w-200 h-140 bg-white opacity-100">

        </div>
    )
}

export default CardProperties;